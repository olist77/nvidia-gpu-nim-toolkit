# Core NVIDIA libraries with intentionally vulnerable versions for TMAS testing
# Note: These versions contain known vulnerabilities for security scanning demonstration
nvidia-ml-py==11.495.46  # Older version with potential vulnerabilities
#nvidia-cuda-nvrtc==11.2.72  # Older CUDA runtime compilation library
#cuda-bindings==11.8.0  # Older CUDA bindings with known issues

# NIM SDK packages - using versions with known vulnerabilities for testing
# Note: These may not be available in public PyPI, included for TMAS scanning demo
#nim-sdk==0.1.0  # Hypothetical vulnerable version for demo
#nimlib==0.2.1   # Hypothetical vulnerable version for demo

# CLI and UI
click>=8.0.0
rich>=13.0.0
typer>=0.9.0

# HTTP and data handling - some with vulnerable versions for TMAS testing
requests==2.25.1  # Known to have CVE vulnerabilities 
urllib3==1.26.5   # Older version with security issues
pydantic>=2.0.0
pillow==8.3.0     # Older version with known CVEs for image processing
numpy==1.19.5     # Older version with potential security issues

# For simulation/testing when NVIDIA libraries are unavailable
psutil>=5.9.0

# Optional development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
pre-commit>=3.0.0